class Ground{
    constructor(x,y,width) {
        this.x = x;
        this.y = y;
        this.width = width;
    }
    display() {
        groundObj= new ground(width/2,670,width,20);
    }
}